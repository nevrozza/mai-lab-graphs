# ЛР. Графы
### Матрица смежности и кратчайший путь алгоритмом Дейкстры

В проекте реализована работа с графом, который хранится с помощью матрицы смежности. Для поиска кратчайшего пути используется алгоритм Дейкстры.

По условию лабораторной работы нужно использовать представление графа через матрицу смежности и реализовать поиск кратчайшего пути.

> [!NOTE]
> Главная сложность работы была не в хранении матрицы, а в аккуратной реализации самого поиска: нужно правильно выбрать стартовую вершину, поддерживать массив расстояний, отмечать уже обработанные вершины и восстановить итоговый путь.

---

# Отчет по лабораторной работе по курсу "Фундаментальная информатика"

**Тема:** Графы. Представление графа и поиск кратчайшего пути.

**Цель:** Освоить представление графа в памяти программы и реализовать алгоритм поиска кратчайшего пути.

**Задание:** Представить граф с помощью матрицы смежности и реализовать алгоритм Дейкстры для поиска кратчайшего пути.

> **Представление графа:** матрица смежности.
>
> **Алгоритм:** кратчайший путь - Дейкстра.

## Идея и метод решения

Граф хранится в виде матрицы смежности. Строки и столбцы матрицы соответствуют вершинам графа, а значение в ячейке показывает наличие ребра и его вес.

Если между вершинами есть ребро, в матрице хранится его вес. Если ребра нет, используется специальное значение, которое означает отсутствие перехода.

Для поиска кратчайшего пути используется алгоритм Дейкстры. Он постепенно находит вершины с минимальным известным расстоянием от стартовой вершины и обновляет расстояния до соседей.

> [!IMPORTANT]
> Алгоритм Дейкстры корректно работает только с неотрицательными весами ребер. Если в графе есть отрицательные веса, нужен другой алгоритм.

## Структура программы

Логически программу можно разделить на несколько частей:

- хранение графа в матрице смежности
- чтение количества вершин и ребер
- заполнение матрицы весами ребер
- проверка корректности введенных данных
- запуск алгоритма Дейкстры
- хранение текущих расстояний от стартовой вершины
- хранение информации о предыдущих вершинах для восстановления пути
- вывод результата

Такое разделение важно, потому что сама матрица отвечает только за хранение графа. Логика поиска кратчайшего пути должна быть отделена от логики ввода и вывода.

## Алгоритм Дейкстры

1. Для всех вершин расстояние считается бесконечным
2. Для стартовой вершины расстояние устанавливается равным `0`
3. Все вершины изначально считаются необработанными
4. На каждом шаге выбирается необработанная вершина с минимальным текущим расстоянием
5. Для всех соседей этой вершины проверяется, можно ли улучшить расстояние
6. Если новый путь короче, расстояние до соседа обновляется
7. Для восстановления пути запоминается предыдущая вершина
8. Вершина помечается как обработанная
9. Алгоритм продолжается, пока есть достижимые необработанные вершины

## Тонкости реализации

Основная сложность была в том, чтобы не просто получить длину кратчайшего пути, а корректно обработать все состояния алгоритма.

Наиболее важные моменты:

- правильно обозначить отсутствие ребра в матрице
- не спутать отсутствие ребра с ребром нулевого веса
- корректно выбрать следующую вершину с минимальным расстоянием
- не обновлять расстояния через уже недостижимые вершины
- хранить предыдущие вершины для восстановления маршрута
- обрабатывать случай, когда путь между вершинами отсутствует
- проверять границы номеров вершин
- не использовать отрицательные веса для Дейкстры

> [!IMPORTANT]
> Отдельная неприятность - восстановление пути. Найти длину пути проще, чем потом аккуратно вывести последовательность вершин от начальной до конечной.

Кратко про обработку ошибок: программа должна учитывать некорректное количество вершин, неправильные номера вершин, отрицательные веса, отсутствие пути между выбранными вершинами и ошибки ввода. В таких случаях нужно вывести понятное сообщение, а не продолжать работу с поврежденными данными.

> [!NOTE]
> Матрица смежности удобна для проверки наличия ребра за `O(1)`, но при большом количестве вершин может расходовать много памяти, потому что хранит данные для всех пар вершин.

## Сценарий выполнения работы

1. Анализ условия лабораторной работы
2. Выбор структуры хранения графа по условию - матрица смежности
3. Реализация ввода графа
4. Реализация заполнения матрицы весами ребер
5. Реализация алгоритма Дейкстры
6. Реализация восстановления кратчайшего пути
7. Реализация вывода длины пути и маршрута
8. Тестирование на обычных и пограничных случаях

Примеры тестов:

| **Сценарий** | **Что проверяется** |
|:------------|:--------------------|
| простой связный граф | базовая работа алгоритма |
| несколько путей между вершинами | выбор минимального пути |
| вершина недостижима | корректное сообщение об отсутствии пути |
| граф из одной вершины | путь из вершины в саму себя |
| ребро с большим весом и обходной короткий путь | корректное обновление расстояний |
| отрицательный вес ребра | обработка некорректного случая |
| неверный номер вершины | проверка границ |
| граф без ребер | отсутствие достижимых путей |

## Сложность

При использовании матрицы смежности и простого поиска минимальной необработанной вершины алгоритм Дейкстры работает за `O(n^2)`, где `n` - количество вершин.

| **Операция** | **Сложность** |
|:------------|:--------------|
| хранение графа | `O(n^2)` |
| проверка ребра между двумя вершинами | `O(1)` |
| поиск кратчайших путей | `O(n^2)` |
| восстановление пути | `O(n)` |

## Выводы

В ходе выполнения лабораторной работы было реализовано представление графа с помощью матрицы смежности и поиск кратчайшего пути алгоритмом Дейкстры.

Были закреплены навыки работы с двумерными массивами, графами, массивами расстояний, пометками посещенных вершин и восстановлением пути.

Главный вывод: матрица смежности делает проверку ребра простой и быстрой, но требует больше памяти. В алгоритме Дейкстры основная сложность связана не только с обновлением расстояний, но и с аккуратной обработкой недостижимых вершин и восстановлением итогового маршрута.